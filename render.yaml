services:
  - type: web
    name: fastapi-backend
    env: python
    pythonVersion: "3.12.0"  # <--- AQUÍ FORZAS 3.12
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
